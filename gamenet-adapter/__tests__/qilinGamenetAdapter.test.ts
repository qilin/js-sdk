import qilinGamenetAdapter from '../qilinGamenetAdapter';

describe('QilinGamenetAdapter', () => {
  describe('Init method', () => {
    it('Method is defined', () => {
      expect(qilinGamenetAdapter.init).toBeDefined();
    });
  });
});
