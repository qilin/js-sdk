import qilinRamblerAdapter from '../qilinRamblerAdapter';

describe('QilinRamblerAdapter', () => {
  describe('Init method', () => {
    it('Method is defined', () => {
      expect(qilinRamblerAdapter.init).toBeDefined();
    });
  });
});
